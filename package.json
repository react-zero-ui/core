{
  "name": "react-zero-ui-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "runtime",
    "init-react-zero-ui"
  ],
  "scripts": {
    "lint": "npm run lint -w runtime",
    "test": "npm run test -w runtime",
    "pack:runtime": "npm pack --workspace runtime",
    "pack:init": "npm pack --workspace init-react-zero-ui"
  },
  "dependencies": {
    "@austinserb/react-zero-ui": "^1.0.13"
  },
  "devDependencies": {
    "@austinserb/react-zero-ui": "workspace:runtime"
  }
}