export namespace CONFIG {
    namespace SUPPORTED_EXTENSIONS {
        let TYPESCRIPT: string[];
        let JAVASCRIPT: string[];
    }
    let HOOK_NAME: string;
    let IMPORT_NAME: string;
    let MIN_HOOK_ARGUMENTS: number;
    let MAX_HOOK_ARGUMENTS: number;
    let HEADER: string;
    let ZERO_UI_DIR: string;
    let CONTENT: string[];
}
export const IGNORE_DIRS: Set<string>;
